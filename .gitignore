# Runtime state
config/agent.conf
data/
memory/
logs/
git/

# Panel build
panel/node_modules/
panel/dist/
panel/tsconfig.tsbuildinfo

# OS
.DS_Store

# Docker Compose — local config with host-specific paths (use docker/docker-compose.example.yml as template)
docker/docker-compose.yml
